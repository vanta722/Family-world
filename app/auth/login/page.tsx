export default function LoginPage() {
  return (
    <section className="mx-auto max-w-md rounded-xl bg-white p-6 shadow">
      <h1 className="text-2xl font-bold">Parent Login</h1>
      <p className="mt-2 text-sm text-slate-600">
        Integrate Supabase Auth UI/provider in next iteration. For now, login wiring is intentionally minimal.
      </p>
    </section>
  );
}
